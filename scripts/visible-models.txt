# [2026-09-04]-[English localization: translate user-facing messages; no logic change]
# opencode-switchman authoritative enablement surface: models shown (visible) in the opencode desktop "Models" manager, one provider/model-id per line; lines starting with # are comments.
#
# This file only affects the static baseline baked into the package at publish time (src/shells.json), namely:
#   1) tier metadata for offline cold start (bundledModelIndex, so unknown models do not degrade to the single "off" tier);
#   2) last-resort fallback for the free safety-net list (normally OpenCode Zen free models take priority, see the fallback logic in src/index.ts);
#   3) the injection list for the static path of matrix.mode=legacy.
# It does NOT limit the models available to npm users: the runtime superset = the user's config surface ∪ all conversable models of providers with credentials ∪ fallback models.
# The active surface is controlled by the user's own visible set/favorites; if a user does not have a model listed here, the related shells are simply not injected/activated, with no side effects.
# It is also NOT a field-tested list: gen:shells performs no connectivity/quality verification; this is merely the enablement surface the maintainer uses day to day.
# User-side availability is dynamically backstopped by the runtime probes (10-minute refresh), which auto-degrade/trigger the breaker when down.
#
# Note: emptying/fully commenting out this file degrades auto mode into a personal environment snapshot of running `opencode models` on the maintainer's machine — worse quality, not recommended.
# After changing model-manager toggles, manually sync this file and re-run bun run gen:shells (included in bun run build / prepublishOnly).
github-copilot/claude-haiku-4.5
github-copilot/claude-opus-5
github-copilot/claude-sonnet-5
github-copilot/gemini-3.1-pro-preview
github-copilot/gemini-3.7-flash
github-copilot/gpt-5.6-luna
github-copilot/gpt-5.6-sol
github-copilot/gpt-5.6-terra
github-copilot/grok-4.6
deepseek/deepseek-v4-flash-vision-exp
deepseek/deepseek-v4-pro
zhipuai-coding-plan/glm-5.3
zhipuai-coding-plan/glm-5.3-flash
